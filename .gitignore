# ── Environment & credentials ─────────────────────────
.env
.env.*
backend/.env
backend/.env.*
frontend/.env.local
frontend/.env.*
!.env.example
!backend/.env.example
!frontend/.env.example
CREDENTIALS.md
*.credentials
.git-credentials

# ── Backend ───────────────────────────────────────────
backend/dist/
backend/node_modules/

# ── Frontend ──────────────────────────────────────────
frontend/.next/
frontend/node_modules/

# ── Landing ───────────────────────────────────────────
landing/.next/
landing/node_modules/

# ── Data & logs ───────────────────────────────────────
data/
*.log
.DS_Store

# ── Local staging secrets & build cache ────────────────
# staging/*.sh and staging/https-proxy.js ARE tracked — see repo history —
# only the self-signed TLS material, runtime pidfiles, and DB backup dump
# are excluded here.
staging/certs/
staging/pids/
backend/sql/.backups/
*.tsbuildinfo
